import React from 'react'

const REQTRAMITE_Mp = ( {tramite} ) => {
  return (
    <div>
        <p>{tramite.name}</p>
        <p>{tramite.price}</p>
        <p>{tramite.description}</p>
    </div>
  )
}

export default REQTRAMITE_Mp